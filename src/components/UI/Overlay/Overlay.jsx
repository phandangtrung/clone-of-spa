export default () => {
  // eslint-disable-next-line unicorn/consistent-function-scoping
  const addOverlay = () => {
    // todo st
  };

  // eslint-disable-next-line unicorn/consistent-function-scoping
  const removeOverlay = () => {
    // todo st
  };

  return { addOverlay, removeOverlay };
};
